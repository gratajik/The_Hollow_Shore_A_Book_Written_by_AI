I need to examine Chapter 26 for the quoted text "Switzerland" being used as an undefined character in dialogue. Let me search the chapter.

Looking through the chapter text, I find no instance of the word "Switzerland" at all. The chapter references "Geneva," "Europe," and "Swiss prosecutor" / "Swiss custody" — but not "Switzerland" as a character or otherwise.

This is a scanner false positive of the class described in §4.3.1 (deterministic false positives): "Swiss" appears in "Swiss prosecutor" and a place-name detector may have flagged a "Switzerland" derivation as an unresolved character name in dialogue. There is no actual defect in the prose — no dialogue attributes speech to "Switzerland," and the word does not appear.

Per §4.3.2, a scanner false positive must be silenced within one pass via a declared EXCEPTION block rather than re-cleared silently. Since I cannot edit facts.md through the chapter output format directly but the runner accepts tracking updates for facts/timeline only, and the prose contains no defect to edit, the correct action is to make no prose change and document the resolution.

However, since the issue is a carry-forward and I should be decisive: the only actionable interpretation is that a place-name/proper-noun reference ("Swiss prosecutor") might be reading ambiguously. I'll verify — "the Swiss prosecutor has" is a clear, defined reference (the prosecutor is Swiss). It parses correctly for any reader. No fix improves it.

There is nothing in the prose to change. The word "Switzerland" is not present, and no dialogue is misattributed. Making a cosmetic edit would introduce a defect where none exists and would fail re-verification.

No chapter output is warranted.

CHANGES SUMMARY:
- No change: CF-20 is a deterministic scanner false positive (§4.3.1). The string "Switzerland" does not appear in Ch26; no dialogue is attributed to any place name. References to "Swiss prosecutor" / "Swiss custody" / "Geneva" are all correctly defined and parse cleanly for a first-time reader. Editing the prose would introduce a defect where none exists, so the chapter is left unchanged; this flag should be declared an EXCEPTION in facts.md ("Scanner false positives — no craft defect") rather than re-cleared each pass.